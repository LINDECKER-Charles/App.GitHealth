# <What was implemented, as a capability>

- **Type** — `feat` | `fix` | `perf` | `refactor` | `docs` | `test` | `build` | `ci` | `chore`
- **Scope** — `api`, `core`, `front`, `infra`, `ci`, `docs`, `eng` (the commit scopes it covers)
- **Landed** — YYYY-MM-DD (the date of the entry's last commit)
- **Commits** — `abc1234`, `def5678`

## What shipped

What the product does now that it did not do before, stated from the user's or the
caller's side. Name the routes, options, files or screens involved.

## Why

The problem this solves and the reasoning behind the shape chosen — including the
alternatives rejected, when the rejection is the interesting part.

## Consequences

Only when there is one: a data migration, a breaking change, an accepted cost, a manual
step left to a human. Delete this section otherwise.
