name: Benchmark

on:
  pull_request:
    paths:
      - "benchmarks/**"
      - "src/App.GitHealth.Api/**"
      - "src/App.GitHealth.Core/**"
      - "Directory.Build.props"
      - "global.json"
      - "App.GitHealth.sln"
      - ".github/workflows/benchmark.yml"
  push:
    branches:
      - main
    paths:
      - "benchmarks/**"
      - "src/App.GitHealth.Api/**"
      - "src/App.GitHealth.Core/**"
      - "Directory.Build.props"
      - "global.json"
      - "App.GitHealth.sln"
      - ".github/workflows/benchmark.yml"
  schedule:
    - cron: "41 3 * * 1"
  workflow_dispatch:
    inputs:
      enforce_budgets:
        description: Échouer si les budgets du runner de référence sont dépassés
        required: false
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: benchmark-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    name: Mesurer 100, 500 et 1 000 branches
    runs-on: windows-latest
    timeout-minutes: 45
    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
      DOTNET_NOLOGO: "1"
      ENFORCE_BENCHMARK_BUDGETS: ${{ inputs.enforce_budgets || false }}

    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v7

      - name: Installer .NET
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json

      - name: Exécuter la baseline
        run: |
          $budgetArguments = @()
          if ($env:ENFORCE_BENCHMARK_BUDGETS -eq "true") {
            $budgetArguments += "--enforce-budgets"
          }
          dotnet run `
            --project benchmarks/App.GitHealth.Benchmarks/App.GitHealth.Benchmarks.csproj `
            --configuration Release `
            -- `
            --sizes 100,500,1000 `
            --warmup 1 `
            --iterations 3 `
            --output artifacts/benchmarks/latest.json `
            @budgetArguments

      - name: Conserver les mesures
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-windows
          path: artifacts/benchmarks/latest.json
          if-no-files-found: error
