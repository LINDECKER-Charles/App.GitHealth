# Auto detect text files and perform LF normalization
* text=auto

# Binary assets: no end-of-line normalisation, on any system.
*.ttf binary
*.ico binary

# Build entry points: the shebang requires LF line endings, cmd.exe requires CRLF.
*.sh text eol=lf
*.cmd text eol=crlf
