/**
 * Identity shown in the top bar. The version follows `Directory.Build.props`,
 * which is what the build stamps on the assemblies: that file, `package.json`
 * and `CHANGELOG.md` are updated together at release time.
 */
export const appVersion = '0.2.0';

export const userGuideUrl =
  'https://github.com/LINDECKER-Charles/App.GitHealth/blob/main/docs/USER_GUIDE.md';

export const databaseBackupUrl = '/api/exports/database';
